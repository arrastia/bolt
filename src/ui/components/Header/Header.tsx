import { Styles } from './Header.styes';

import { DynamicIsland } from 'ui/components/DynamicIsland';

export const Header = () => {
  return (
    <Styles.Header>
      <DynamicIsland />
    </Styles.Header>
  );
};
