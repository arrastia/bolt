import styled from 'styled-components';

const Wrapper = styled('div')`
  bottom: 0;
  left: 0;
  overflow: hidden;
  pointer-events: none;
  position: fixed;
  right: 0;
  top: 0;
  z-index: 1;
`;

export const Styles = { Wrapper };
