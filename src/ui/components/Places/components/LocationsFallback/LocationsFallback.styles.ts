import styled from 'styled-components';

const Fallback = styled('div')`
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  height: 100%;
`;

export const Styles = { Fallback };
